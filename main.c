#iunclude "pwd.h"

int main(void)
{
    char buffer[1024];
    if (my_open(secret.csv))
    {
        create_file();
    }
    while(1)
    {
        buffer = read_line(0) || get_next_line(0);
        if()
    }
}